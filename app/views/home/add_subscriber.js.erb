<% if @errors.present? %>
  toastr.error("<%= @errors %>")
<%else%>
  toastr.success("<%= t('shoppe.subscribers.subbscribed_successfully') %>")
  localStorage.setItem("modalhidden", "hidden");
  setTimeout(function(){ window.location = "/"; }, 3000);
<% end %>
