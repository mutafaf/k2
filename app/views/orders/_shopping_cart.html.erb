<div>
<% if order.order_items.present? %>
  <% order.order_items.each do |item| %>
     <div class="col-md-2 col-sm-4 col-xs-6">
          <img src='<%= cart_item_image(item.ordered_item) %>' width='80px' alt='Not found' class="img-responsive center-block" alt="" draggable="false">
     </div>
     <div class="col-md-10 order-item">
        <div class="col-md-3 col-sm-3 col-xs-6 order-size allan"> 
        <% if item.ordered_item.has_sizes? %>
             <strong>Size: </strong>
             <%= select_tag 'size', options_for_select(item.ordered_item.get_available_sizes.collect(&:name), item.size), :class=> "qty size-select", :onChange=>"updateOrderItem(this)" %>
           <% end %>
           <h3> <%= item.ordered_item.get_name %></h3><strong><%= number_to_currency item.unit_price %> </strong>
           <br><br>
           <%= hidden_field_tag :item_id,{}, :value=>item.id  %>
           <!-- <p class="details-cart">peltro distressed (H38222)</p> -->
          
        </div>
        <div class="col-md-3  col-sm-3 col-xs-12  order-size allan">
        <% if item.ordered_item.has_available_colors? %>
          <strong><span class="qty-font">COLOR: </span></strong>
          <%= select_tag 'color', options_for_select(item.ordered_item.get_available_variants.collect{ |v| [v.name, v.id] }, item.ordered_item.id), :class=> "qty size-select", :style=>"width:40%;", :onChange=>"updateOrderItem(this)" %>
        <% end %>
        </div>
        <div class="col-md-3  col-sm-3 col-xs-6 order-size  allan">
           <strong><span class="qty-font">QTY:</span> </strong>
           <%= select_tag 'quantity', options_for_select(Shoppe::Product::QUANTITY, item.quantity), :class=> "qty ", :onChange=>"updateOrderItem(this)" %>
           <span class="remove-qty">
           <button class="removerow" onclick="updateOrderItem(this)">Remove</button>
           </span>
        </div>
        <div class="col-md-3  order-size allan total-text text-center"><strong>Total</strong><div class="cart-total text-center "><%= number_to_currency item.sub_total %> </div></div>
     </div>
     <div style="clear:both"></div>
     
  <% end %>
<% else %>
  <div class="text-center"><h3>Your cart is empty.</h3></div>
<% end %>
</div>
